int arr[1000];

int double(int n) {
    return n * 2; //no recursion, just a simple function to demonstrate function calls
}

void main() {
    int target;
    i = 0;
    
    // Push a value
    arr[i] = 42;
    i = i + 1;

    int res;
    res = double(5);
    write(res);

    write(arr[i]);
}